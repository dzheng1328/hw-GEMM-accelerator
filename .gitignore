# Simulation artifacts
sim_build/
obj_dir/
*.vcd
*.vvp
*.fst
*.fst.hier
!docs/waveforms/*.fst
*.log
!synth/reports/*.log
results.xml

# Python
__pycache__/
*.pyc
.pytest_cache/
venv/
.venv/

# Yosys / OpenLane outputs
*.json
!config/*.json
!openlane/**/config.json
runs/

# Model training artifacts (frozen model/mnist_quantized.npz IS committed)
model/checkpoints/
model/.mnist_cache/

# Editor / OS
.DS_Store
.vscode/
*.swp

# Third-party PDK data (fetched by synth/fetch_sky130.sh, never committed)
synth/lib/

# Third-party OpenRAM source clone (fetched by openram/run_operand_sram.sh, never committed)
openram/.openram-src/

# Local PDK_ROOT for OpenRAM's sky130 SRAM bitcell setup (symlink + third-party
# clones, fetched by openram/run_operand_sram.sh, never committed)
openram/.pdk-root/

# Manual DRC/LVS diagnostic scratch workspaces for the operand_mem SRAM macro
# (large regenerable artifacts: extracted .mag/.ext files, LVS reports/netlists)
openram/diag_tmp/
openram/klayout_verify/

# Subagent-driven-development scratch workspace (ledgers, briefs, review packages)
.superpowers/

# Git worktrees for isolated implementation work
.worktrees/

# AI agent working files (instructions, plans/specs)
CLAUDE.md
AGENTS.md
docs/superpowers/
